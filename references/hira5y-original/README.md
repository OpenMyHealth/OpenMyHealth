# hira5y original source snapshot

This directory contains an unmodified source snapshot copied from Persly's existing `hira5y` and `nicePhoneCertification` scraping implementation.

Purpose:
- Preserve working reference behavior and request sequencing.
- Support browser-port parity checks for openChart.

Notes:
- Files here are reference-only and are not compiled into extension runtime.
- Browser runtime implementation lives in `src/hira/*`.
